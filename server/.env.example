# TEMPLATE ONLY — not loaded at runtime.
# Copy to a file named `.env` in this same folder:
#   cp .env.example .env
# Then put your real values in `.env`. The app loads `.env`, not this file.

SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-jwt
